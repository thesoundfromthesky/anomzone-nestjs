export enum NodeEnv {
  Development = 'development',
  Local = 'local',
  Production = 'production',
}

export enum ValidationGroup {
  Get = 'GET',
  List = 'LIST',
  Create = 'CREATE',
  Update = 'UPDATE',
  Delete = 'DELETE',
}

export enum CategoryGroup {
  Post = 'post',
  Comment = 'comment',
  Reply = 'reply',
}
